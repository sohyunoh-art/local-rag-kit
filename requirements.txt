# 개발용 의존성 목록. 일반 사용자는 `pip install .` (또는 git+https) 만으로 충분하다.
# 핵심 검색 엔진(청킹·BM25·answerer)은 표준 라이브러리만으로 동작한다.

# PDF 인제스트용 (pyproject 의 기본 의존성과 동일)
PyMuPDF>=1.24

# 테스트
pytest>=8.0

# 선택: 완전 오프라인 LLM 답변을 원하면 Ollama 를 별도 설치
#   1) https://ollama.com 에서 설치
#   2) ollama pull llama3.2   (또는 원하는 모델)
#   3) ragkit ask ... --backend ollama
